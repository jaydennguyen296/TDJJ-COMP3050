name: comp3050-dev

services:
  server:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    working_dir: /workspace
    command: bash -c "nginx -g 'daemon off;'"
    ports:
      - "80:80"
    expose:
      - "8000"
    volumes:
      - ..:/workspace
