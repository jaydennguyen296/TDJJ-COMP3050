apiVersion: v1
kind: Service
metadata:
  name: comp3050-server
  labels:
    app: comp3050-server
spec:
  type: NodePort
  selector:
    app: comp3050-server
  ports:
    - name: http
      port: 80
      targetPort: 8000
      # nodePort omitted: cluster assigns one in 30000–32767
